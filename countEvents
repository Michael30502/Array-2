public int countEvens(int[] nums) {
  int equalNumbers =0;
  
  for (int i=0; i<nums.length; i++) {
    if (nums[i]== 0)
    equalNumbers ++;
    else if(nums[i] !=1)
    if (nums[i] % 2  == 0)
    equalNumbers++;
  }
  return(equalNumbers);
  
}
